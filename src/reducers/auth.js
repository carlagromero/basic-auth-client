const INITIAL_STATE = {
  authenticated: '',
  errorMeesage: '',
}

const authReducer = (state = INITIAL_STATE, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default authReducer;